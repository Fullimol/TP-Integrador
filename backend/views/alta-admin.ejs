<!DOCTYPE html>
<html>
<head>
  <title>Alta Usuario Administrador</title>
  <link rel="stylesheet" href="/styles/altaymod-producto.css">
</head>
<body>
  <form id="formAlta" action="/usuarios/alta-admin" method="POST">
    <h1>Alta Usuario Administrador</h1>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email">

    <label for="password">Contraseña:</label>
    <input type="password" id="password" name="password">

    <div class="botones-container">
        <button type="submit" class="aceptar-button">DAR ALTA</button>
        <button type="button" class="cancelar-button" id="btnCancelar">CANCELAR</button>
    </div>
  </form>

  <% if (error) { %>
      <p style="color: red; text-align: center;"><%= error %></p>
  <% } %>
  

  <script>
    const form = document.getElementById('formAlta');
    form.addEventListener('submit', e => {
      const email = form.email.value.trim();
      const password = form.password.value.trim();

      });

      document.getElementById('btnCancelar').addEventListener('click', () => {
        window.location.href = '/juegos/dashboard';
      });
  </script>
</body>
</html>
