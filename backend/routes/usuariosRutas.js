const express = require('express');
const router = express.Router();
// const {  loginUsuario } = require('../controllers/usuariosController');


// router.post('/login', loginUsuario);

module.exports = router;